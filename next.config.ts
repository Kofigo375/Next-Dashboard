import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // experimental, was trying out partial prerendering 
 // experimental: {
  // ppr: 'incremental'
  //}
};

export default nextConfig;
